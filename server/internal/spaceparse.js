//read YAML


var Space = function(spec) {
  
};

_.extend(Space.prototype, {
  type: function(){
    // if army_moves is empty, this is a water space

    // else, it's land

  }


});



// create

// read YAML

// parse once to create all the objects

  // for each space in map, create a space object

// parse again to link them up
